name: flua
description: "Embed Lua 5.4 in Flutter applications via Dart FFI"
version: 0.0.1

environment:
  sdk: ^3.11.5

dependencies:
  code_assets: ^1.0.0
  ffi: ^2.1.4
  hooks: ^1.0.0
  logging: ^1.3.0
  native_toolchain_c: ^0.17.4

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
